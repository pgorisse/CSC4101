{{ form_start(form) }}
    {{ form_widget(form) }}
<p>
    <button class="btn btn-outline-success"><i class="fa fa-check"></i> {{ button_label|default('Save') }}</button>
</p>
    {{ form_end(form) }}
